# AGENTS.md

## Cursor Cloud specific instructions

This repository (`lang-academy`) is currently an empty project initialized with only a `README.md`. There are no source files, dependencies, build systems, or services configured yet.

- **No dependencies to install** — no `package.json`, `requirements.txt`, or similar manifest exists.
- **No services to run** — no backend, frontend, or database services are configured.
- **No tests or linting** — no test framework or linter is set up.
- **No build step** — no build tooling is present.

Once the project scaffolding is added, this section should be updated with setup, run, lint, test, and build instructions.
